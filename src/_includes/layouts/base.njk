<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {%- set css -%}{%- include "styles.css" -%}{% endset -%}
    <style>{{ css | cssmin | safe }}</style>
    <title>{%- if title -%}{{ title }}{%- else -%}Chowkhat{%- endif -%}</title>
    <meta name="description" content="{%- if description -%}{{ description }}{%- else -%}Chowkhat - Indian handmade clothing, kurtis store.{%- endif -%}"/>
  </head>
  <body>
    <header>
      <div class="container">
        <a href="/" class="home" title="Home"><img src="/images/chowkhatlogo.png" class="logomark" alt="Chowkhat"></a>
        {% block header %}
          {# Replace this if anything is passed in to the header block #}
          {% if header %}
          <h1>{{ header.heading }}</h1>
          <p>
            {{ header.subtitle }}
          </p>
          {% else %}
          <h1>
            🎉 {% if details.twitter %}{{ details.twitter }}{% else %}We{% endif %} are India's first affordable handmade store.
          </h1>
          <p>
            Get mesmerized
          </p>
          <p>
            <a href="/#get-yours" class="btn btn-white">Check us out</a>
          </p>
          {% endif %}
        {% endblock %}
      </div>
    </header>
    <div class="main">
      <div class="container">
        {% block content %}
        {{ content | safe }}
        {% endblock %}
      </div>
    </div>
  </body>
</html>
